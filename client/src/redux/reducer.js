



const initialState= {
    countries: [],
    countryDetail: [],
    countrySelected: null,
    pagina: 1,
    activities: []
};

const reducer = (state = initialState , { type, payload}) => {
    switch (type) {
        
            
        
    
        default:
            return { ...state}
    }
};

export default reducer;

